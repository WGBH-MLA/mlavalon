I"‰(function() {
  $(function() {
    var initialized;
    $(document).on('click', 'a[data-trigger="submit"]', function(event) {
      return $(this).closest('form').submit();
    });
    initialized = false;
    return $('#browse-btn').browseEverything().show(function() {
      var alertMsg, skip_box;
      alertMsg = $('<div>').html('Warning! Uploading too many files at once can lead to ingest failures.').addClass('alert').addClass('alert-danger').css('margin', '3px').css('text-align', 'center');
      $('.ev-body').prepend(alertMsg);
      if (!($('#browse-everything input[name=workflow]').length > 0)) {
        skip_box = $('#web_upload input[name=workflow]').closest('span').clone().removeClass().css('margin-right', '10px');
        $('.ev-cancel').before(skip_box);
      }
      $('.ev-providers .ev-container a').click();
      return initialized = true;
    }).done(function(data) {
      if (data.length > 0) {
        $('#uploading').modal('show');
        $('#dropbox_form input[name=workflow]').val($('#browse-everything input[name=workflow]:checked').val());
        return $('#dropbox_form').submit();
      }
    });
  });

}).call(this);
:ET