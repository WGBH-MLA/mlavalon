I"D(function() {
  $(function() {
    var refreshToken;
    refreshToken = function() {
      var mount_point, token;
      token = currentPlayer.media.src.split('?')[1];
      if (token && token.match(/^token=/)) {
        mount_point = $('body').data('mountpoint');
        return $.get(mount_point + "authorize.txt?" + token).done(function() {
          return console.log("Token refreshed");
        }).fail(function() {
          return console.error("Token refresh failed");
        });
      }
    };
    return setInterval(refreshToken, 5 * 60 * 1000);
  });

}).call(this);
:ET