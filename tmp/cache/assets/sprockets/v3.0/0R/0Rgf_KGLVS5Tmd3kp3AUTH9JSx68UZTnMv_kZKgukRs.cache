I";(function() {
  $('.date-input').datepicker({
    dateFormat: 'yy-mm-dd'
  });

  $('#ajax-modal').on('shown.bs.modal', function(event) {
    $('.date-input').datepicker({
      dateFormat: 'yy-mm-dd',
      onSelect: function() {
        $(this).change();
      }
    });
    $('.access_date').on('change', function(event) {
      if (this.value !== '') {
        $(this).closest('label').find('.remove_access').prop('disabled', true);
      } else {
        $(this).closest('label').find('.remove_access').prop('disabled', false);
      }
    });
  });

}).call(this);
:ET